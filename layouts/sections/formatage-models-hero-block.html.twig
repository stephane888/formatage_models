{% if content %}
	<div{{attributes.addClass('hero block')}}>
		{% if content.main %}
			<div{{region_attributes.main.addClass('main')}}>
				{{ content.main }}
			</div>
		{% endif %}
	</div>
{% endif %}